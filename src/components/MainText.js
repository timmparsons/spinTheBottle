import React from 'react';

const MainText = () => (
  <div className="mainTextSection">
    <p className="mainTextPara">Want to try something new? Give the wheel a spin and we'll choose one for ya.</p>
  </div>
)

export default MainText;